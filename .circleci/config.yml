version: 2.1
jobs:
  build:
    machine:
      image: circleci/classic:201708-01
    environment:
      CKANVERSION=2.8
    steps:
      - run:
          name: where am I
          command: pwd && ls -la
      - run:
          name: install
          command: /bin/travis-build.bash
      - run:
          name: test
          command: /bin/travis-run.sh
