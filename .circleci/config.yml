jobs:
  build:
    docker:
      - image: circleci/python:2.7
    environment:
      - CKANVERSION=2.8
    steps:
      - run:
        name: install
        command: bin/travis-build.bash
      - run:
        name: test
        command: bin/travis-run.sh
